name: x_operator_agent_dev

services:
  workspace:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      # プロジェクトフォルダを丸ごと同期（コード編集が即時反映）
      - .:/workspace
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    stdin_open: true
    tty: true